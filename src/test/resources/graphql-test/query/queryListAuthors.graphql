query ListAuthors($authorIds: [String!]!) {
    listAuthors(authorIds: $authorIds) {
        id
        name
        books {
            id
            name
            rating
            isbn
        }
    }
}